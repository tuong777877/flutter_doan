import 'package:flutter/cupertino.dart';

class DifferentFragment extends StatelessWidget {
  static String routeName = "/Different_screen";
  const DifferentFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
